export const TypingConfig = {
    // Speed settings (milliseconds per character)
    baseSpeed: 50,
    fastSpeed: 30,
    slowSpeed: 200,
    verySlowSpeed: 400,
    
    // Haptic feedback settings
    hapticWordMinLength: 3,
    enableHaptics: true,
    
    // Animation settings
    cursorBlinkSpeed: 530,
    loadingDotsSpeed: 500,
    
    // Delays
    responseStartDelay: 500,
    clearChatDelay: 800,
    testResponseDelay: 300,
  };